#pragma once

// WiFi credentials — edit before flashing
#define WIFI_SSID "your_wifi_ssid"
#define WIFI_PASS "your_wifi_password"

// IP of the PC running Unreal Engine (must be on same subnet)
#define OSC_HOST  "192.168.1.100"

// Unreal OSC server port — set to match your OSC Server node in Blueprint
#define OSC_PORT  8000
